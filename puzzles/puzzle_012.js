const num = 10;

function getFibonacci(number) {
    let str ='';
    let fib = 1;
    for(let i = 0; i < 10; i++) {
        if(i === 0 || i === 1) {
            str += `${i} `;
        }
        
    }
}